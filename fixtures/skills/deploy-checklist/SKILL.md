---
name: deploy-checklist
description: "Walk through the pre-deploy checklist: run migrations, flip the feature flag, and notify the on-call channel. Use when the user says 'ready to deploy' or 'pre-deploy checklist'. Examples: '/deploy-checklist'."
---

# Deploy Checklist

Step through migrations, feature flags, and the on-call notification before a production release.
